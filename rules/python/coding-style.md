# Python コーディングスタイル

対象ファイル: `**/*.py`, `**/*.pyi`

## 基本規約
- **PEP 8** に従う
- すべての関数シグネチャに **型アノテーション** を付ける

## データ構造
- ミュータブルなクラスより `@dataclass(frozen=True)` や `NamedTuple` を優先する

## コードツール
- フォーマット: `black`
- インポート整理: `isort`
- リント: `ruff`
