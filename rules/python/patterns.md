# Python パターン

対象ファイル: `**/*.py`, `**/*.pyi`

## プロトコル（ダックタイピング）
- 継承を使わず `Protocol` で構造的な型を定義する

## データクラス（DTO）
- `@dataclass` デコレータでデータ転送オブジェクトを作成する
- 型ヒントとデフォルト値を明示する

## リソース管理
- ファイル・DB接続などのリソースは `with` 文（コンテキストマネージャー）で管理する
- 大量データの反復処理はジェネリクスでメモリ効率を確保する
