# プロジェクト状態確認

現在のプロジェクトの状態を日本語で報告してください。以下を順番に確認してください：

1. `git status` を実行して未コミットの変更を確認
2. `git log --oneline -5` で直近5件のコミット履歴を確認
3. `git stash list` でスタッシュが残っていないか確認
4. package.json または requirements.txt などが存在する場合、最終更新日を確認
5. プロジェクト内の TODO / FIXME コメントを検索して件数を報告

最後に「次にやるべきこと」を1〜3点、簡潔にまとめてください。
